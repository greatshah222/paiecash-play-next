export const metadata = {
	title: "Admin/Create | Edit event",
	description: "Admin/Create | Edit event",
};

const CreateEventLayout = ({ children }) => {
	return children;
};

export default CreateEventLayout;

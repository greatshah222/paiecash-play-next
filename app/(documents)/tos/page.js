import Container from "@/components/ui/container";
import TOSClient from "./components/tos-client";

const TOSPage = () => {
	return (
		<div className="bg-white">
			<Container className={"bg-white"}>
				<TOSClient />;
			</Container>
		</div>
	);
};

export default TOSPage;
